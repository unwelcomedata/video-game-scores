# Dependencies actually used by this project (fun-tier pop-culture dataset).
# Data pulled from the IGDB API (not scraped), cleaned in DuckDB, charts rendered
# with the shared Pillow chart toolkit.

# --- core data ---
pandas==3.0.5
duckdb==1.5.5
pyarrow==25.0.0
numpy==2.2.6

# --- IGDB API ingestion ---
requests==2.32.3
python-dotenv==1.1.1      # Twitch/IGDB credentials from a gitignored .env

# --- config ---
pyyaml==6.0.2

# --- chart rendering ---
Pillow==12.3.0            # shared Pillow-based chart toolkit
